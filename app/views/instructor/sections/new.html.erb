<div class="registration_box col-10 offset-1">
  <h1 class="text-center"> Add A New Section </h1>

  <%= simple_form_for @section, url: instructor_course_sections_path(@course) do |f| %>
    <%= f.input :title %>
    <%= f.submit "Add Section", class: 'btn btn-primary' %>
  <% end %>
</div>